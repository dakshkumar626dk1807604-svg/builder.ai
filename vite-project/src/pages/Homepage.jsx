import react from 'react'

/**
 * Homepage component that serves as the main landing page.
 * @returns {JSX.Element} The homepage interface
 */
const Homepage = () => {
  return (
    <div>HomePage </div>
  );
}
export default Homepage